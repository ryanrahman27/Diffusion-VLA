"""LIBERO evaluation + statistics for the diffusion-backbone study (Sec. 9.1).

All variants are evaluated under identical LIBERO conditions (same suites,
deterministic initial-condition sets, action space, horizon, normalization).
Simulation removes calibration / latency confounds and makes high-n cheap.
"""
